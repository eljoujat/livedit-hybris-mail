/**
 * Created by eljoujat on 11/21/15.
 */





/*
editor = CodeMirror.fromTextArea($("textarea[ytestid='script']"), {
    mode: "text/x-impex",
    lineNumbers: true,
    autofocus: true,
    extraKeys: {
        "F11": function(cm) {
            setFullScreen(cm, !isFullScreen(cm));
        },
        "Esc": function(cm) {
            if (isFullScreen(cm)) setFullScreen(cm, false);
        },
        "Ctrl-Space": "autocomplete"
    }
});
*/


/*$("textarea[ytestid='bodyScript']").on('blur',function(){
    console.log("new value is "+$(this).val())
})*/


